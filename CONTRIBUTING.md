# Contributing

Use a pull request against `main`. Run locked restore, Release build and non-hardware tests before opening it. Do not add screenshots, images, videos, logs, diagnostic archives, credentials, local paths or device identifiers to the repository.

Hardware claims require reproducible evidence from an approved device test; mocked tests alone are not hardware confirmation.
